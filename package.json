{
  "name": "collab-code-desktop",
  "version": "1.0.0",
  "description": "Desktop-based collaborative coding and student performance tracking platform",
  "private": true,
  "workspaces": [
    "apps/client",
    "apps/server",
    "apps/worker"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/server\" \"npm run react-start --workspace=apps/client\" \"npm run dev --workspace=apps/worker\" \"wait-on http://localhost:3000 && npm run electron-start --workspace=apps/client\"",
    "dev:old": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "start": "npm start --workspace=apps/server",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.0.1"
  },
  "keywords": [
    "collaborative",
    "coding",
    "education",
    "real-time",
    "desktop"
  ],
  "author": "Your Name",
  "license": "MIT"
}
